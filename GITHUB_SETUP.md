# 📦 Загрузка на GitHub

## Шаг 1: Создать репозиторий на GitHub

1. Зайди на [GitHub](https://github.com/)
2. Нажми **New repository** (зелёная кнопка)
3. Заполни:
   - **Repository name**: `ai-trading-bot` (или любое имя)
   - **Description**: `AI-powered crypto trading bot with 8-level safety`
   - **Visibility**: ⚠️ **PRIVATE** (чтобы никто не увидел твою стратегию!)
   - **НЕ** ставь галочку "Add a README" (у нас уже есть)
4. Нажми **Create repository**

---

## Шаг 2: Загрузить код

### На своём ПК (PowerShell):

```powershell
# Перейти в папку проекта
cd C:\Users\yusif\OneDrive\Desktop\trader

# Инициализировать Git (если ещё не сделано)
git init

# Добавить все файлы (кроме .env - он в .gitignore!)
git add .

# Сделать первый коммит
git commit -m "Initial commit: AI Trading Bot with 8-level safety"

# Подключить к своему репозиторию
git remote add origin https://github.com/ТВОЙ_USERNAME/ai-trading-bot.git

# Загрузить на GitHub
git push -u origin main
```

**Если будет ошибка "main branch doesn't exist":**
```powershell
git branch -M main
git push -u origin main
```

---

## Шаг 3: Проверить что .env НЕ загрузился!

1. Открой свой репозиторий на GitHub
2. Убедись что файла `.env` НЕТ в списке файлов
3. ✅ Должен быть `.gitignore` который блокирует .env

⚠️ **КРИТИЧНО:** Если увидишь `.env` - удали репозиторий и пересоздай! Там твои API ключи!

---

## Шаг 4: Обновление кода в будущем

Когда изменишь код:
```powershell
git add .
git commit -m "Описание изменений"
git push
```

---

## 🔄 Клонирование на другой ПК/сервер

```bash
git clone https://github.com/ТВОЙ_USERNAME/ai-trading-bot.git
cd ai-trading-bot

# Создать .env вручную (его НЕТ на GitHub!)
nano .env
# Вставить ключи

# Установить зависимости
python -m venv .venv
source .venv/bin/activate  # Linux
# ИЛИ
.venv\Scripts\activate  # Windows
pip install -r requirements.txt

# Запустить
python trading_bot.py
```

---

## 📝 .gitignore защищает:

✅ `.env` - API ключи  
✅ `trading_history.db` - история сделок  
✅ `__pycache__/` - Python кеш  
✅ `.venv/` - виртуальное окружение  
✅ `optimization_results_*.csv` - результаты тестов  

Эти файлы НЕ будут загружены на GitHub!

---

## 🎯 Итог

✅ Код в безопасности на GitHub (приватный репозиторий)  
✅ .env с ключами НЕ загружен (защищён .gitignore)  
✅ Можно клонировать на VPS/другой ПК  
✅ История изменений сохранена  
✅ Бекап на случай поломки ПК  
