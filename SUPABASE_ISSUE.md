# Проблема с Supabase

## ❌ Текущая ошибка:
```
401 Unauthorized - Invalid API key
```

## 🔍 Диагностика:

Ключи в .env выглядят правильно (JWT токены, 207 символов), но Supabase их не принимает.

## 🛠️ Решения:

### Вариант 1: Переполучить ключи

1. Открой: https://supabase.com/dashboard/project/ixovpisndoyhsaaqlypl/settings/api

2. Найди **Project API keys** → **service_role** → **secret**

3. Нажми на иконку копирования (📋) - скопируй ПОЛНЫЙ ключ

4. Вставь сюда ВЕСЬ ключ одной строкой

### Вариант 2: Отключить RLS (Row Level Security)

В SQL Editor выполни:
```sql
ALTER TABLE signals DISABLE ROW LEVEL SECURITY;
ALTER TABLE trades DISABLE ROW LEVEL SECURITY;
ALTER TABLE performance DISABLE ROW LEVEL SECURITY;
```

### Вариант 3: Использовать только SQLite пока

Бот уже работает с локальной SQLite базой данных. Supabase - дополнительная опция для облачного хранения.

## 📊 Текущий статус:

- ✅ SQLite база работает
- ❌ Supabase подключение блокируется 401
- ✅ Код готов для двойного хранения

## 💡 Рекомендация:

Пока Supabase не подключена, бот сохраняет всю информацию в локальную `trading_history.db`. Это полностью функционально.

Supabase добавим позже когда получим рабочие ключи.
